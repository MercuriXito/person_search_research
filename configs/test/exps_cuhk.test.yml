# freeze training experiments:
# - CUHK-SYSU; 
# - w/ ACAE
# - frozen backbone
# - contrastive loss

train:
  output_dir: exps/exps_test
  batch_size: 2
  loss_weights:
    loss_graph: 10.0
  epochs: 1
model:
  name: retinanet
  graph_head:
    loss:
      name: "contrastive"
      margin: 0.35

